# The Last General's Mind — Python dependencies
# Python 3.12.4 (verified on project machine: python3 --version)
#
# Install:
#   pip install -r requirements.txt
#
# Test:
#   python3 -m pytest tests/ --tb=short -q
# Expected count changes as work progresses — check state/PROGRESS.md for
# the current figure rather than trusting this comment, which goes stale
# on every commit that adds tests. Last known-correct at commit time:
# 413 passed (Candidate E E1 Step 2, in progress).

pytest==9.0.2
pytest-cov==7.1.0
time-machine==2.16.0
Faker==37.6.0
anyio==4.13.0
